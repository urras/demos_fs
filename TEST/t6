echo "
Проверка работоспособности редактора текстов ed.

Для проверки используется файл 'text', содержащий несколько предложений
на русском языке. Задачей редактирования является замена всех слов,
состоящих только из латинских букв на эти же слова, но взятые в кавычки.
Результат работы редактора ed записывается в файл 'done', который
затем сравнивается с эталонным файлом 'text.std'.
В зависимости от результата сравнения выдается сообщение о совпадении
или несовпадении текстов. Содержимое файла 'text':
	"
cat text
echo "
	ИДЕТ РЕДАКТИРОВАНИЕ ...
	"
ed - text<<!
g/[A-Za-z].[A-Za-z]*/s//"&"/g
w done
q
!
echo	"
	РЕЗУЛЬТАТ РЕДАКТИРОВАНИЯ (файл 'done'):
	"
cat done
if diff -b done text.std
then 	echo	"
	Файл отредактирован ПРАВИЛЬНО.
	"
else	echo	"
	Файл отредактирован НЕВЕРНО !!!
	"
	stat=bad
	exit 1
fi
